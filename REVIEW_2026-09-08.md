# feature/fast-switch コードレビュー (2026-09-08)

このファイルは git に入れない作業メモ(.git/info/exclude に登録済み)。
対象: feature/fast-switch を origin/master と比較した全変更(30 ファイル、約 2200 行)。
確認方法: 4 系統のレビューを並列で実施し、重要な指摘はコードを直接読んで再検証した。

実機ログ(SD カード HDZGOGGLE.log / HDZGOGGLE.prev.log、Async Display の ON/OFF 比較)も参照した。

---

## 0. 引き継ぎ (2026-09-10 更新)

このセクションだけ読めば作業を始められる。**この fork が何をしているかの説明は
`docs/fork-changes.md` (git 追跡下、第三者に見せられる形)。** このファイルは
調査の過程と実測値の置き場で、時系列に積んである。

### 0.0 2026-09-10 時点の状態

- HEAD = 417950c "Re-fit the menu when Playback takes the display to 1080p"。
  origin には push していない。
- out/ と SD カードは HEAD のビルドで一致確認済み。
- **このセッションで扱った不具合はすべて実機で解決を確認済み。** 内訳:

  | 症状 | 結果 | 章 |
  |---|---|---|
  | 映像視聴中のメニュー復帰が 2 秒 | 6〜33ms に | 14 |
  | チャンネル変更が 2 秒 | 同上(同じ原因) | 14 |
  | メニューのレイアウト崩れ | 解決 | 14.4 |
  | Playback の斜めずれ | 解決 | 15.2 |
  | Playback 一覧の縮小 | 解決 | 16.4 |
  | 起動が 12.9 秒 | 316ms 待って回避、実証済み | 16.1 |
  | 720p Playback | 実機で失敗、撤去 | 16.2 |

- **次にやること: 第 2 章・第 3 章の残り 12 件。** 棚卸し済み(第 17 章)。
  効果が明確なのは 2.1(アンチエイリアスが映像に戻っても復帰しない)と
  2.4(ステータスバーが 20Hz で全画面再描画)。

### 0.1 環境
- ブランチ feature/fast-switch。ゴーグルは develop.sh 経由で SD 上の HDZGOGGLE を
  優先実行する。カードを抜けば純正に戻る。
- このファイルは .git/info/exclude で除外済み。コミットに含めないこと。
- ビルド: arm64 Mac では setup.sh が動かない。以下の amd64 コンテナで増分ビルドする(数分)。
  ```
  docker run --rm --platform linux/amd64 -v "$PWD":/src -w /src debian:bookworm-slim bash -c '
    apt-get update -qq >/dev/null 2>&1 && apt-get install -y -qq cmake make >/dev/null 2>&1
    cd build && make all -j$(nproc) 2>&1 | grep -E "error|warning:|generated out|Built target HDZGOGGLE" | grep -v liblog.so'
  ```
  成功したら `cp out/HDZGOGGLE "/Volumes/NO NAME/HDZGOGGLE"` して cmp で一致確認。Release は -Werror なので未使用変数に注意。
  **ファイルを追加・削除したら cmake を再実行すること** — CMakeLists.txt は
  src/ui/*.c を configure 時にしか glob しない。
- コミットは変更ごとに。末尾の Co-Authored-By は使用モデルの指示に従う。

### 0.2 ユーザーの方針(必ず守る)
- **この fork が変えたことは、すべてメニューのスイッチで元に戻せること。**
  各改造は速度ページの On/Off 行にし、既定は OFF。Performance ページは
  2026-09-09 に Boot Speed / Switch Speed / Input Feel に分割済み。
- 例外にできるのは「戻す対象の体験が無い」ものだけ(ログの数値、壊れた設定の
  拒否、UI が同一のリファクタ)。その場合も単独コミットにして revert 一発で
  戻せる形にする。
- 起動時間の比較は実機ログで行う。数字の根拠は必ずログの時刻で示す。推定は「推定」と書く。
- レビュー結果はこのファイルに残す。

### 0.3 ログの読み方
- SD カード直下の HDZGOGGLE.log(最新)と HDZGOGGLE.prev.log(1 つ前)。
- 時計は rtc_init で 1970 年から 2026 年へ飛ぶ。電池が無いので毎回同じ保存値に戻り、**04:01:55.0 付近が全起動共通のアンカー**になる。2026 側の時刻は起動間で直接比較できる。
- "boot total: app start to video Nms" は CLOCK_MONOTONIC なので信頼できる(8892507 以降)。
- 主要マーカー: "boot phase: ...", "switch mark: ...", "System Execute: Nms for ...", "vdpo: ...", "HDZero: async open started/collected", "SPI: burst refused", "wifi: stop skipped", "DM6302 after init"。
- 映像が出る瞬間 = "switch mark: display mode set"。映像 = max(dispw 終了, メインスレッド終了) + 約 85ms。

### 0.4 作業リスト(優先順)

> **2026-09-09 更新: A〜H はすべて cb7ec5e で実施済み。実機未検証。**
> 次にやることは第 9 章の検証だけ。以下は経緯として残す。


**A. 音声前倒しの判定を「実行中」に直す(必須、起動で 500ms 損している)**
- src/core/app_state.c:287 の `vdpo_timing_pending()` は「開始して未回収」。dispw 子プロセスが終わっていても真。
- 修正案: hardware.c の vdpo_worker が system_exec から戻った直後に `vdpo_running = false` を立てる(開始時に true、pthread_create の前に設定)。`bool vdpo_timing_running(void)` を追加し、app_state.c:287 はそれを見る。volatile か __atomic で可視性を確保。
- 期待ログ: async_tuner OFF の起動で "audio and dvr while the display changes" が出ず、映像が dispw 終了の約 85ms 後に来る。

**B. バースト書き込みの部分成功を失敗扱いに(必須、1 行)**
- src/driver/i2c.c i2c_write_burst: I2C_RDWR は成功メッセージ数を返す。`ret != count` を失敗とし、errno が 0 なら -EIO を返す。

**C. 初期化失敗時のバス速度復帰と試行回数(推奨)**
- src/driver/dm6302.c DM6302_init: to_cnt >= 10 の return 1 の前に 200kHz 復帰(i2c_bus_lock(2) + aww 0x58)を入れる。元からの穴。
- src/driver/hardware.c HDZero_open: ワーカーが retry_tuner_init で 2 回失敗した後、メインスレッドがさらに 2 回走らせる。ワーカーの失敗をフラグで残し、回収直後の HDZero_open は最大 1 回に抑える。

**D. WiFi スキップの条件(推奨、影響は小)**
- src/ui/page_wifi.c:317 はドライバ有無だけ。wlan_stop.sh は dropbear/rtspLive/hostapd/udhcpd の killall もする。アプリ再起動 + SSH ON で dropbear が 2 重起動。
- 案: スキップ時も `killall dropbear rtspLive hostapd udhcpd 2>/dev/null` を 1 回だけ system_exec する(sleep 1 が無いので約 15ms)。または ssh 有効時はスキップしない。

**E. Performance ページの節約表示を実測に合わせる(推奨)**
- src/ui/page_performance.c の SAVING_*。第 8.7 節の表の通り。特に Async Tuner Init "est. -900ms" → 「Async Display 併用時 ほぼ 0」、Skip WiFi Stop → 「映像後」、Preload OSD Fonts → 「600〜1000ms(フォントの場所による)」。

**F. dispw を起動直後に始める(効果最大 約 -364ms、実機検証が必要)**
- 新しいトグル(例: speed.boot_display_early、"Early Video Timing")として追加。既定 OFF。
- 条件: boot_display ON かつ skip_boot_menu ON かつ async_display ON かつ boot_goes_to_hdzero_video()。boot_display OFF だと Display_UI_init が 1080p50 の dispw を追加で走らせ合計 3 回になるので必須条件。
- 場所: src/core/main.c の HDZero_open_async_start の隣。app_state.c の start_display_timing_early() を非 static にして呼ぶ。CAM_MODE は起動時点で既定値 VR_720P60 で、切り替え時点でも同じなので前倒しで悪化しない。
- 危険: vdpo_start_timing_async() は先頭で OLED_display(0) を呼ぶが、その時点では OLED_Startup() 前。未初期化パネルへの off が無害か不明。引数で「パネルを触らない」を選べるようにするか、実機で確認する。OLED_Pattern が dispw 実行中にパネルを on にするので、一瞬乱れる可能性あり(skip_boot_menu で画面は黒)。
- 期待: dispw が約 55.35 開始、56.48 終了。切り替え開始時に回収済みで、映像は約 2481ms。これをやって初めて Async Tuner Init が意味を持つ。

**G. Goggle V1 の保護(推奨)**
- GOGGLE_VER_2 == 0 の個体は OLED_write/OLED_read が AL FPGA(0x65)経由。Async Tuner Init ON だと起動時の OLED 初期化が丸ごと 1MHz で走る。stock はこの経路を持たない。
- 案: async_tuner を GOGGLE_VER_2 == 0 では無視する。加えて main() 起動時に GOGGLE_VER_2 と getHwRevision() を LOGI する(今のログには版数が出ていない)。

**H. コメントの修正(軽微)**
- src/driver/dm6302.c:31「毎回同じ場所で 1 回拒否」は誤り(0 回の起動あり)。
- src/core/main.c:232「FPGA 以外は別のバス」は誤り。初期化も表示/OLED もポート 2。
- src/driver/hardware.c:592 のガードのコメント「リセット中か初期化中」→「未回収の間」。

### 0.5 やらなくてよいこと
- 受信機初期化、SPI バースト、音声設定のこれ以上の高速化。メインスレッドは dispw 待ちで 364ms 遊んでいるので、F をやるまで起動時間には出ない。
- ワーカーの nice+10 が EFUSE1 を 294ms 遅くしている件(第 8.4 節)。同じ理由で今は無害。

### 0.6 検証チェックリスト(次の起動ログで見る)
- "HDZero: async open collected" が出る(ワーカーが動いた証拠)。
- "boot phase: display and osd" が 340〜430ms。1000ms 台なら I2C 独占が再発。
- "SPI: burst refused" は 0〜1 回、"burst disabled" は出ない。"DM6302 after init" は left=0x0 right=0x0。
- A の後: async_tuner OFF で "audio and dvr while the display changes" が出ない。
- "boot total" を前回(ON 2976ms / OFF 3559ms)と比べる。

---

## 1. 起動時間の実測内訳 (Async Display ON、映像まで 3.83 秒)

RTC セット (04:01:55.007) からの経過。時計は rtc_init で 1970 年から飛ぶので、それ以前は 1970 側で比較する。

| 区間 | 時間 | 備考 |
|---|---|---|
| アプリ開始 → rtc_init (1970 側) | 139ms | settings 30ms、language_init 47ms(全言語を解析し全文字列を LOGD)、pclk cp/rm 26ms(存在しない SD ファイル) |
| device_init (IT66021/IT66121/TP2825/DM5680) | 185ms | |
| gpadc_init (aww ×2) | 24ms | |
| lvgl_init | 15ms | |
| main_menu_init | 589〜679ms | 裏スレッド 2 本と CPU を取り合う |
| statusbar + 初回描画 | 41ms | |
| OLED_Startup(温度テスト) | 54ms | 2 回目の読み出しは書き込みがなくても走る |
| Display_UI_init (aww ×5 = 45ms) | 54ms | |
| OLED_Pattern (off→on の usleep) | 61ms | |
| osd_init (フォントは先読み済み) | 158ms | 1080p 用セットも常に構築 |
| 切り替え開始 (aww, OLED off, HDZero_Close) | 46ms | |
| **DM6302_init** | **1795ms** | クリティカルパス最大。usleep は 101ms だけで残りは I2C 転送回数 |
| dispw 720p60 (裏スレッド) | 1077ms | DM6302_init に完全に隠れた |
| SetChannel + vldflg | 58ms | |
| Display_720P60_50 (aww ×3、OLED on) | 88ms | ここで映像が出る |
| lvgl 720p 切替 + OSD | 74ms | 映像後 |
| audio_sel.sh in_mic2 + out_off | 477ms | 映像後、メインループを止める |
| wlan_stop.sh(post-bootup) | 1077ms | `sleep 1` が全て。映像後 1.1 秒間 OSD と入力が止まる |

Async Display OFF との差: dispw が直列で 1092ms → 切り替え区間 3.70s → 2.55s、全体 4.87s → 3.83s(約 1.04 秒短縮)。

---

## 2. 不具合 (修正推奨)

### 2.1 アンチエイリアスが映像に戻っても復帰しない
- src/ui/ui_main_menu.c:369-373 `main_menu_apply_antialiasing()` は `menu_antialias_off && menu_scaled && main_menu_is_shown()` で判定。
- 復帰は `main_menu_show(false)` の分岐にあるが、`main_menu_show(false)` はどこからも呼ばれない。メニューは OSD 画面に覆われるだけで HIDDEN にならない。
- 条件: Menu Antialias OFF + Menu Over Video + 720p 映像。映像に戻っても `disp_drv.antialiasing` が 0 のままで OSD と全ウィジェットがギザギザ。
- 修正案: app_exit_menu() で `menu_scaled`/表示中を無視して復帰させるか、実際にメニューを HIDDEN にする。

### 2.2 オーバーレイメニュー中にカメラ解像度が変わると OSD がメニューの上に出る
- src/core/osd.c:479-498 `fhd_change()`、osd.c:637 で `osd_hdzero_update()` から無条件に呼ばれる。
- keep_display では `Display_UI()` を呼ばないため `source_mode` が HDZERO のまま。`HDZERO_detect()` が動き続け、720p↔1080p 変化で `fhd_req` が立つ。
- `fhd_change()` が `osd_show(true)` するのでメニューが OSD の下に隠れる。ダイヤルはメニューを操作し続け、拡大率も古いまま。
- 修正案: `g_app_state == APP_STATE_VIDEO` のときだけ `fhd_change()` を走らせる。

### 2.3 お気に入りの帯域外スロットが「L1」等と表示される
- src/core/osd.c:352-355 `channel2str()` は範囲外チャンネルで先頭要素を返す。
- src/ui/page_favorites.c:80 で帯域外(例: Raceband で登録した F 帯を Lowband で表示)のスロットが本物の L1 と区別不能。`slot_usable()` はそれを飛ばすので、見えているのに選べない。
- 修正案: `slot_label_update()` で帯域外を明示表示。

### 2.4 ステータスバーの差分判定が省略記号付きラベルで常に失敗
- src/ui/ui_statusbar.c:49-58 `sb_label()` は `lv_label_get_text()` と比較。
- LVGL 8.3 の LONG_DOT はラベル内部文字列を「…」で上書きする(lib/lvgl/lvgl/src/widgets/lv_label.c:1140-1150)。長い WiFi SSID などで毎回不一致 → 毎回 `lv_label_set_text` → 20Hz で全画面再描画(拡大メニューなら 5.6MB レイヤーの再生成)。
- 修正案: バー側で最後に設定した文字列を保持して比較する。

### 2.5 IMU 初期化失敗時もゲートを開ける
- src/core/main.c:279 `ht_set_imu_ready()` は無条件。`enable_bmi270()` は void で失敗をログするだけ。
- 失敗時は 100Hz の SIGEV_THREAD タイマーが毎回 `get_bmi270()` の無限ループスレッドを生み、i2c_mutex を占有。元のファームと同じ挙動だが、ゲートで直せる位置。
- 修正案: `enable_bmi270()` に戻り値を持たせ、成功時のみ `ht_set_imu_ready()`。
- 補足: ht.c:190-194 のコメント「FPGA とバスを共有」は誤り。BMI270 はポート 1、FPGA はポート 2。共有しているのは i2c_mutex だけ。

### 2.6 system.c の実行時間計測が壁時計のまま
- src/util/system.c:14-19 `elapsed_ms()` が gettimeofday。time.c を CLOCK_MONOTONIC にした理由と同じで、rtc_init をまたぐ計測が壊れる。`time_ms()` を使う。

---

## 3. リスク (状況次第で問題)

### 3.1 HDMI 経路が dispw の記録を迂回
- src/driver/hardware.c:1288-1400 `HDMI_in_detect()` の 7 箇所が直接 `system_exec("dispw ...")` し `vdpo_tmg` を直接代入。`vdpo_pending`/`vdpo_applied_once` を触らない。
- HDMI オーバーレイ中に HDZero へ切り替えると、非同期 dispw と HDMI 側 dispw が同時実行され得る。`vdpo_collect()` は後勝ちを無視して pending 値で上書き。
- HDMI は `vdpo_applied_once` を立てないので、boot_display + HDMI 起動 + ソース切替で 1.1 秒の冗長 dispw が走る。
- 修正案: 7 箇所を `vdpo_set_timing()` 経由にする。hardware.c:677-678 のコメント「stale にならない」は半分だけ正しい。

### 3.2 fast_menu スタンバイの受信機が HDMI セッションや OLED 保護中も通電
- src/core/app_state.c:167-198 `app_switch_to_hdmi_in()` は HDZero を閉じない(stock は menu で reset していた)。
- src/core/ht.c:110-113 OLED 保護は `source_mode == HDZERO` のときだけ閉じるので、通常メニュー中(UI)のスタンバイ受信機は 1 分保護中も通電。
- 修正案: `app_switch_to_hdmi_in()` と OLED_MD_PRE_OFF で `hdz_standby` なら `HDZero_Close()`。

### 3.3 オーバーレイメニュー中の OLED 保護 → 復帰時にメニューが約 2 秒固まる
- ht.c:134-137: `source_mode == HDZERO` のまま 1 分無操作で受信機 close、次の動きで `DM6302_init` + `SetChannel` をメインループ(lvgl_mutex 保持)で実行。
- 修正案: ht.c の判定を `source_mode` ではなく `g_app_state` で「メニュー表示中」を UI 扱いにする。

### 3.4 オーバーレイ中の Playback ページ(**2026-09-09 実機で再現、修正済み — 第 15.2 節**)
- 表示ソースが映像のままなので、SoC の VO レイヤーが FPGA で映像と合成される可能性。実機テストか、Playback 進入時に非オーバーレイ経路を強制する。

### 3.5 thread_version が lvgl_mutex なしで lv_timer_handler() を呼ぶ(元からの問題)
- src/ui/page_version.c:1159-1215 → src/ui/ui_main_menu.c:564-595 `progress_bar_update()`。LVGL をロック外で触る唯一の経路。

### 3.6 timed_long_press はリピートイベントが来たときしか判定しない
- src/core/input_device.c:495-521。100/200ms 設定はカーネルのリピート周期(推定 250ms 遅延 + 33ms)に丸められる。EV_REP のないデバイスでは長押しがクリックに退化。

### 3.7 skip_boot_menu が背景色を黒のまま戻さない
- src/ui/ui_main_menu.c:463-469。起動専用設定がセッション中ずっとメニューの見た目を変える。

### 3.8 boot_fonts が SD マウント前にフォントを読む可能性
- osd.c:1107/1111。stock より 1〜2 秒早く SD を読む。遅いマウントだとカスタムフォントが黙って内蔵にフォールバック。ログの "failed!" で判別可能。

### 3.9 skip_audio のキャッシュは record デーモンがミキサーを触らない前提
- dvr.c:50 の前提はアプリ内では真だが、mkapp/app/app/record のバイナリ(audio-setup.txt に amixer cset の記述)は検査不能。

### 3.10 INI のお気に入り値が範囲チェックなし
- src/core/settings.c:289-293。手編集 setting.ini で `ch1=60`(analog)だと `channel2str()` の analog 分岐で配列外参照 → `lv_label_set_text` にゴミポインタ。`count` も uint8_t 切り詰め後に 1..8 判定。ロード時にクランプする。

### 3.11 Performance ページの見出しスクロールがコメント通りに動いていない
- page_performance.c:349-354。見出しパネルは HIDDEN でレイアウトされず座標が親原点のまま。最初の呼び出しでリスト先頭へ飛ぶ。見た目だけの問題。

### 3.12 確定前のお気に入り/スライダー編集がメモリだけに残る
- page_favorites.c:220,239 / page_performance.c:344。フォーカス中に長押しで抜けると INI に保存されず、次回起動で戻る。stock の fans ページも同じ。

---

## 4. パフォーマンス改善案 (効果 ÷ リスク順)

| # | 案 | 見込み | リスク | 状態 |
|---|---|---|---|---|
| 1 | 受信機初期化を UI 構築と同時に開始(device_init 直後にスレッド、SetChannel 前に join) | 約 0.9〜1.0 秒 | 中: SPI 7 転送の排他、TWI2 が 1MHz の間 OLED/AL も同速度 | **実装済 (Async Tuner Init)** |
| 2 | SPI_Write の 7 転送を 1 回の I2C_RDWR に(dm6302.c:57-80、i2c_write_n は未使用) | 0.6〜0.9 秒 | 中: FPGA がリピートスタートを受けるか未確認。初期化後の読み戻しで即判明 | **実装済 (Burst Tuner Writes)** |
| 3 | 起動時 wlan_stop.sh をスキップ(page_wifi.c:310、`sleep 1` が全コスト) | 映像後 1.08 秒 | 低 | **実装済 (Skip WiFi Stop)** |
| 4 | audio_sel.sh をスレッド化 / amixer 1 プロセス化 / libasound ioctl 直叩き(lib/softwinner に libasound.so あり) | 切り替え 0.48 秒 | 低 | 未着手 |
| 5 | EFUSE1 を 2 チップ同時読み(dm6302.c:1238-1362、SPI_Read は両チップ返す) | 0.33 秒 | 中: band_num がチップで異なる可能性 | 未着手 |
| 6 | aww のフォークを /sys/class/sunxi_dump/write への書き込みに置換(起動経路 13 回 ≒ 130ms、切替往復 100〜250ms) | 0.13 秒+ | 低 | 未着手 |
| 7 | IT66021/IT66121/TP2825 初期化をスレッド化(185ms 窓) | 0.15 秒 | 低(TP2825 は 1MHz 前に) | 未着手 |
| 8 | 起動時の OLED on/off 往復をやめる(OLED_Pattern で on にしてすぐ dispw で off) | 60ms | 低〜中 | 未着手 |
| 9 | 1080p OSD セットを遅延生成(osd_init 158ms の半分) | 70〜80ms | 中: img_arr[1] 参照のガード | 未着手 |
| 10 | OLED_Startup 温度テストの 2 回目読み出しを書き込み時のみに | 25〜50ms | 低 | 未着手 |
| 11 | language_init を使用言語のみ + LOGD 削除 | 45ms | なし | 未着手 |
| 12 | pclk_phase の cp/rm を access() で回避 | 26ms | なし | 未着手 |

その他: DM6302_init の 100ms リセット待ちは根拠なし(ループが再試行するので 20ms でも自己修復)。Init2 が freq 0 に同調してすぐ SetChannel で再同調(約 40ms)。settings_load の 96 回のファイル再オープン(約 30ms)。record.conf を切替ごとに 3 回書き換え(dvr_update_vi_conf)、setting.ini の last_source をチャンネル変更ごとに書き換え。`dvr_enable_line_out(false)` を page_source が直後に true に戻すため skip_audio のキャッシュが無効化される(page_source.c:172/185/193)。REC_STOP_LIVE(gogglecmd)が毎切替で走る。app_switch_to_analog/av_in の sleep(1)、hdmi_in の sleep(2)。

### 常時負荷
- 映像モード: osd.c:703-713 でアンテナアイコンを毎パス `lv_img_set_src()`(無条件)。`lv_img_set_src` は即 invalidate + BMP ヘッダ再読み込み。`full_refresh=1`(ui_porting.c:159)なので 100Hz 上限で全画面描画 + 3.75MB memcpy。差分判定で映像中のアイドル CPU をほぼゼロにできる。
- メニューモード: ui_statusbar.c:160 の SD カードラベルが LONG_SCROLL_CIRCULAR。文字列が 267px を超えるとスクロールアニメが毎ティック invalidate → 拡大メニュー全体(5.6MB ARGB レイヤー)を最大 100Hz で再生成。LONG_DOT にするだけで解消。
- ui_porting.c:49-54 の flush が area->x1 を無視するため full_refresh 必須。部分描画対応で RSSI アイコン更新が 64×64 ブリットになる(中リスク)。

---

## 5. 細かい点
- settings.h ease_use_t 内に fast_menu の説明コメントだけが取り残されている。
- hardware.c:602-609 で同じ文が 2 回。
- dm6302.c:1761-1769 のコメントは存在しないチェックの説明。初期化ごとに 10 転送の読み出しを追加している(記録のみ)。
- app_state.c:66-70 / settings.h:256-257 は DM6302_init を 2.3 秒と書くが実測は 1.8 秒。
- page_performance.c:65-66「13 行」→ 実際は 24 行。create_heading の buf コピーは不要。
- page_common.h MAX_PANELS 10→26 で全 22 ページに隠しパネルが増加(約 570 オブジェクト、30〜40KB)。
- ui_main_menu.c:89 MENU_STOCK_COUNT = PAGE_COUNT-3 のマジック。
- ui_throttle で低電圧ビープ間隔が約 0.1 秒 → 1 秒に変わる(未記載)。
- ui_statusbar.c:67-71 のコメントは statusbar_show の直上で「誰も隠さない」と言っている。
- ui_porting.c:180-183: antialiasing=0 は拡大メニューだけでなく全ウィジェットの角丸・影もギザギザにする(lv_draw_sw_blend.c:145)。
- msp_displayport.c:466-500 osd_traffic_report は行ごとに memcmp + 5 秒ごと LOGI(何も使っていない)。
- system.c の実行時間ログは有用。Display_UI() の 4 つの switch mark は system_exec の計測と重複。
- 一部ファイルのモードが 100755→100644 に変わっている。

---

## 6. 検証済みで問題なしと判断したもの
- INI キーの load/save 一致(favorites / speed / bugfix / input)。
- 新ページの行数とパネル数、Back 行の選択可能性、ロック下でのコールバック実行。
- ロック順序 lvgl → hardware → i2c に逆転なし。vdpo_start_timing_async は自分で hardware_mutex を取り、保持中に呼ばれる経路なし。非同期 dispw は同じ app_switch_to_hdzero 内で必ず回収される(初期化失敗・default ケース含む)。
- boot_display のスキップは rc.sh:21-22 が先に 1080p50 を設定しているので安全。
- standby/open/close/帯域変更/sleep/scan の相互作用(帯域変更で再初期化、go_sleep と Source_AV は close して hdz_standby をクリア、Display_VO_SWITCH(1) が M0 を再オープン)。
- time.c の CLOCK_MONOTONIC 化: 全呼び出し元は差分のみ使用。LVGL tick の 32bit オーバーフローも解消。
- IMU 非同期初期化: BMI270 はポート 1、同時初期化される機器とは別ポート。ゲートは join 後に立つ。
- split_lock: 段間で共有する状態は handler が触らない。ただし効果は未計測。

---

## 7. 実測メモ (2026-09-09、Async Tuner Init / Burst Tuner Writes / Skip WiFi Stop の初回ビルド)

### 起きたこと
- 3 項目 ON の起動 3 回のうち 2 回で、ワーカーが自分自身を pthread_join して固まっていた(musl は自己 join を検出せず永久待ち)。受信機初期化はメインスレッドが直列で実行し、ログには "async open started" だけが残る。→ コミット "Make Async Tuner Init actually run" で修正(スレッドローカルの自己判定、pending を create 前に立てる)。
- ワーカーが動いた 1 回は全体で +523ms 遅くなった(boot total 4318 → 4841ms)。内訳: UI 構築 719→1057ms(CPU 競合)、display and osd 322→1008ms(同じポート 2 の I2C を単一ミューテックスで取り合い、ワーカーが連続転送で独占)、IMU 待ち 0→94ms(ポート 1 も同じミューテックス)。切り替え自体は 2.0s → 1.27s と速くなったが、受信機が先に終わると dispw(1.15s)がクリティカルパスに残る。
- 対策: ポートごとのロック + ターンスタイルで公平化、ワーカーを nice +10、dispw 待ちの間に audio_sel/dvr 設定(0.5s)を先に実行。
- Burst Tuner Writes: 毎回 Init4 の入口付近で 1 回だけ ioctl が拒否され、前後は成功する。ブリッジが前の SPI コマンドを送出中に次のバーストが隙間なく到着するためと推定。初回ビルドは 1 回目の拒否で恒久無効化していたので、効果は Init0〜Init3 のみ(純粋な書き込み区間 91ms → 34〜40ms、約 2.5 倍)。→ 300us 後に 1 回再試行、その回だけ単発書き込みに退避、20 回で無効化に変更。
- Skip WiFi Stop: 期待通り。"wifi: stop skipped at start-up" が出て、映像直後の 1091ms のメインループ停止が消えた。

### 次に見るべき数字
- "HDZero: async open collected" が出ること、その直前の "waited Nms" と "boot phase: display and osd" が 338ms 前後に戻ること。
- "SPI: burst refused" の回数(1 回なら想定内)と "DM6302 after init" の読み戻し。
- "switch mark: audio and dvr while the display changes" が出て、"to_hdzero done" が "display mode set" の直後に来ること。

---

## 8. 精査 (2026-09-09、Async Tuner Init ON/OFF の 2 回起動)

### 8.1 前回報告の訂正: Async Tuner Init の実効果はほぼゼロ

「583ms 短縮」は誤り。差の大半は 8.2 の自作バグが OFF 側を遅くしたもの。

両起動とも app 開始は RTC 基準で 54.870 / 54.885(15ms 差)なので直接比較できる。

| | 切替開始 | dispw | 受信機初期化 | 映像 |
|---|---|---|---|---|
| ON | 56.461 | 56.497→57.630 (1132ms) | 55.344→56.737(UI 構築と並行) | 57.715 = 2845ms |
| OFF | 56.518 | 56.552→57.644 (1091ms) | 56.552→57.632(dispw と 99% 並行) | 58.293 = 3408ms |

OFF でも受信機初期化と dispw は元から 99% 重なっていた。**Async Display だけで受信機は完全に隠れており、Async Tuner Init が隠す余地はもう無い。**
8.2 のバグを除くと OFF の映像は約 57.758(2873ms)、ON との差は **28ms**。

2x2 の推定(OFF 側は実測からの外挿):

| | tuner OFF | tuner ON |
|---|---|---|
| display OFF | 3965ms | 3121ms |
| display ON | 2873ms | **2845ms** |

単独では async_display -1092ms、async_tuner -844ms。両方でも **-1120ms** で、816ms 分が重複している。

### 8.2 自作バグ: 音声設定の前倒しが映像を遅らせる

app_state.c:287 の `vdpo_timing_pending()` は「開始して未回収」であって「実行中」ではない。dispw の子プロセスは 57.644 に終了したが、回収は 58.204。判定は 57.669 の時点で真のままだったため、隠す相手がいない音声/DVR 設定 535ms が映像の前に入った。OFF 側の 3408ms のうち 535ms がこれ。

- メニュー→映像(受信機は起動済み、dispw だけ 1.1 秒)では正しく隠れるので、この経路では実際に得をする。起動時だけ裏目。
- 正しい直し方: dispw の生存を実際に見る(waitpid WNOHANG など)か、音声設定自体をスレッド化して最後に join する(改善案 4)。後者が上位互換。

### 8.3 Burst Tuner Writes は効いているが起動時間には出ない

受信機初期化(Init0→読み戻し)の実測:

| | 時間 |
|---|---|
| バースト無し(9/8 のログ) | 1663ms |
| バースト有り・競合なし(OFF 起動) | 948ms |
| バースト有り・UI 構築と競合(ON 起動) | 1256ms |

**-715ms(43%)**。ただし dispw の裏なので起動時間には現れない。効くのは fast_menu OFF でのメニュー復帰、OLED 保護からの復帰(メインループ上で全初期化が走る)、スキャンページ。

バースト拒否は ON で 1 回、OFF で 0 回。dm6302.c:31 のコメント「毎回同じ場所で 1 回拒否される」は OFF の回が反証している。要修正(コメント)。

### 8.4 ワーカーの nice+10 は受信機初期化自身を 294ms 遅くする

EFUSE1 1: 465ms(ON)vs 259ms(OFF)、EFUSE1 2: 352 vs 264。Init14 以降の末尾は 261 vs 260 で同一。遅くなるのは main_menu_init と重なる EFUSE1 だけで、これは `while (efuse_rdy) SPI_Read(...)` のポーリングがターンスタイルと nice で毎回譲るため。**今は無害**(メインスレッドは dispw 待ちで 364ms 遊んでいる)。

### 8.5 私の新規コードの問題点(コード未変更)

**要修正**
1. **app_state.c:287** 音声前倒しの判定が古い(8.2)。実効性の判定に変える、または音声をスレッド化する。
2. **i2c.c:220** `ret < 0 ? -err : 0`。`I2C_RDWR` は成功したメッセージ数を返すので、7 通中 3 通しか通らなくても成功扱いになる。最後の 0x90(コマンドレジスタ)が落ちると SPI 書き込み自体が起きず、再送もフォールバックも走らない。`ret != count` を失敗とすべき。errno が 0 のときに `-0` を返す穴も同じ行。
3. **hardware.c:925 + dm6302.c:1739** 初期化失敗時の二重化。`retry_tuner_init` ON でワーカーが 2 回失敗すると `hdzero_open=0` で戻り、メインスレッドがさらに 2 回走らせる(計 4 回、各 10 リセット周期)。加えて **`DM6302_init` の失敗 return は 200kHz への復帰を飛ばす**(元からの穴)ので、ポート 2 がセッション中 1MHz のまま残る。非同期化でその窓が起動 UI 構築まで広がった。
4. **page_wifi.c:317** スキップ条件がドライバの有無だけ。`wlan_stop.sh` は `gogglecmd -live quit` と rtspLive/hostapd/udhcpd/**dropbear** の killall もしている。SSH ON でアプリだけ再起動するとドライバは無いのに dropbear が生きており、`page_wifi_update_services()` (page_wifi.c:238) が 2 つ目を起動する。

**軽微**
- **i2c.c:35** `i2c_bus_lock(port)` は範囲チェック無しでヘッダに公開。内部呼び出しは全て事前に `iic_is_port_ready()` を通るので今は安全。
- **dm6302.c:1719/1808** バスロックを `system_exec`(fork+exec+waitpid、実測 11〜12ms)を跨いで保持。nice+10 のスレッドが持つロックを nice 0 のメインスレッドが待つ優先度逆転。1 回の初期化で 2 回。`aww` をやめない限り避けられない(改善案 6)。
- **dm6302.c:53** 拒否ログの errno は 1 回目の値。2 回目の戻り値は捨てている。ガード失敗の `-1` は「errno 1」と表示される。
- **dm6302.c:31** 8.3 のコメント誤り。
- **hardware.c:592** `HDZero_open_pending()` は「未回収」であって「実行中」ではない。コメントの「リセット中か初期化中」は不正確。害はない(DM6302_M0 が最後に 0x7FC=0 を書く)。
- **hardware.c:966,988** ワーカーの `g_hw_stat` 書き込みは無同期。今は `Display_VO_SWITCH(0)` が `sel` で短絡するため偶然無害。
- **main.c:232** コメント「FPGA 以外は別のバス」は誤り。初期化はまるごとポート 2 で、重ねている表示/OLED もポート 2。だからこそターンスタイルが要った。
- **main.c:91** `boot_goes_to_hdzero_video()` が `start_running()` の分岐条件を二重に持つ。片方だけ変わるとワーカーが回収されないまま残る。
- **time.c:27** `start_s == 0` を「未初期化」の番兵に使っているが、CLOCK_MONOTONIC では起動 1 秒目に本当に 0 になる。実機はカーネル起動 14 秒後にアプリが立つので到達しないが、壁時計版には無かった曖昧さ。
- **page_performance.c** の節約表示(8.7)。

### 8.5.1 検証してリスクと判断したハード面
- **AL FPGA (0x65) が新たに 1MHz で叩かれる。** `pclk_phase_set` の `I2C_Write(ADDR_AL, 0x14, ...)` は Display_UI_init 内。stock はこのデバイスを 1MHz で叩く経路を持たない。2 回の起動では問題なし。Async Tuner Init の主要な残存リスク。
- **バースト再送の安全性は「値が同一だから」成り立っている。** 将来 `spi_write_regs` を値の違う用途に使うと前提が崩れる。

### 8.6 検証して問題なしと判断したもの

- **バースト再送の安全性**: M0 イメージは `SPI_Write(0, 0x3, i << 2, dat[i])` で 1 語ごとに明示アドレス指定(FIFO ではない)。EFUSE は 0x7D0 への読み出しトリガで、再発行しても再読み出しするだけ。よって重複書き込みは全て冪等。
- **ロック順序**: hardware_mutex → spi_mutex → バスロック の一方向のみ。逆順の経路なし。dm6302.c は hardware_mutex を取らない。
- **1MHz バス窓**: `app_switch_to_hdzero` は HDZero_open を hardware_mutex 無しで呼び、thread_peripheral は HDZERO_detect/AV_in_detect/HDMI_in_detect でポート 2 を叩く。つまり **stock でもメニュー→映像のたびにポート 2 が 1MHz 中に他デバイスと共有されている**。ワーカーは新種のリスクではなく、起動時に窓を広げただけ。
- **ワーカーの寿命**: spawn 条件は `start_running()` が HDZero 映像へ直行する場合のみ。その経路は必ず HDZero_open を通り、そこで join される。create_threads より前に必ず回収済み。
- **DM5680 UART**: コマンド単位で cmd_to5680_mutex により直列化。
- **自己 join**: `__thread hdz_in_worker` で回避。帯域変更で worker が HDZero_Close を通る経路も検証済み。
- **Display_VO_SWITCH のガード**: 初期化中の M0 クローズを飛ばすが、DM6302_M0() 自身が最後に 0x7FC=0 を書くので結果は同じ。

### 8.7 Performance ページの節約表示の監査

| 表示 | 実測 | 判定 |
|---|---|---|
| Async Tuner Init "est. -900ms" | 単独 -844ms、Async Display 併用時 **-28ms** | 誤解を招く |
| Async Display "-1100ms" | 単独 -1092ms、Async Tuner 併用時 -276ms | 単独では正しい |
| Skip WiFi Stop "-1080ms" | 1080ms だが**映像より後**のメインループ停止 | 種類が違う |
| Preload OSD Fonts "-1000ms" | 実測 546〜615ms | 約 1.8 倍過大 |
| Skip Audio Setup "-460ms" | 起動時は初回なので必ず実行、**起動には効かない** | 場面の注記が要る |
| Burst Tuner Writes "7 > 1 I2C" | 受信機初期化 -715ms(起動には出ない) | 機構の説明で時間ではない |
| Skip Display Setup / Menu Over Video | dispw は各ログに 1 回のみ、1091〜1132ms | 妥当 |
| Async Motion Sensor "-686ms" | IMU 736〜738ms、待ち 0ms | 妥当 |

**同じ 816ms を 2 つのラベルが別々に主張している**のが構造的な問題。節約値は「単独で」なのか「他と併用して」なのかで変わる。

### 8.8 今のクリティカルパスと次の一手

映像まで 2845ms の内訳(ON 起動):

| ms | 項目 |
|---|---|
| 474 | 初期化前半(settings, rtc, iic, gpio, uart, input, device_init) |
| 40 | 各種 init + ワーカー 2 本の起動 |
| 595 | **main_menu_init**(メインスレッドで露出している最大項目) |
| 50 | statusbar + 初回描画 |
| 430 | OLED_Startup + Display_UI_init + osd_init |
| 38 | ヘッドトラッカー、IMU ゲート、aww、アナログ電源 |
| 240 | ワーカー join の残り |
| 36 | チャンネル同調 |
| 493 | 音声 + DVR 設定 |
| **364** | **dispw 待ちの完全な遊び時間** |
| 85 | pclk / aww / OLED_SetTMG / OLED on |

映像 = max(dispw 終了, メインスレッド終了) + 85ms。メインスレッドに 364ms の余裕があるため、**受信機・SPI バースト・音声のこれ以上の高速化は起動時間に対して価値ゼロ**。

残る手は 2 つだけ:
1. **dispw を起動直後に開始する**(main.c の HDZero_open_async_start の隣で start_display_timing_early を呼ぶ)。dispw は 55.35 開始・56.48 終了となり、切り替えが始まる前に完了する。映像は **約 2481ms** の見込み(-364ms)。これをやって初めて Async Tuner Init も意味を持つ。
   - 前提: boot_display が ON であること。OFF だと Display_UI_init が 1080p50 の dispw を追加で走らせ、合計 3 回になる。
   - CAM_MODE は起動時点で既定値 VR_720P60。切り替え時点でも VTX 報告前なので同じ値。前倒しで悪化はしない。
   - **要検証**: `vdpo_start_timing_async()` は先頭で `OLED_display(0)` を呼ぶが、この時点では `OLED_Startup()` がまだ走っていない。未初期化パネルへの display off が無害かは実機で確認が必要。
2. **dispw 自体を速くする**(1.1 秒の中身は未調査)。


---

## 9. 実施記録 (2026-09-09、コミット cb7ec5e "Act on the review, and add Early Video Timing")

第 0.4 章の A〜H をすべて実施。ビルド済み、SD にコピー済み、**実機未検証**。

### 9.1 やったこと

| 項目 | 内容 | ファイル |
|---|---|---|
| A | `vdpo_child_running` をワーカーが `system_exec` から戻った直後に落とし、`vdpo_timing_running()` で公開。切り替えの音声前倒し判定をそれに変更 | hardware.c/h, app_state.c |
| B | `i2c_write_burst` が `ret == count` 以外を失敗に。`ret >= 0` の部分成功は -EIO、errno 0 も -EIO | i2c.c |
| C | `DM6302_init` の諦め return の前に 200kHz 復帰。`dm6302_bus_speed()` に集約。`hdz_async_init_failed` でワーカー失敗後の二重リトライを抑止 | dm6302.c, hardware.c |
| D | WiFi スキップ時も `killall dropbear rtspLive hostapd udhcpd` を 1 回実行(sleep と rmmod だけ省く) | page_wifi.c |
| E | 節約表示を実測に。「単独での値」であることを明記 | page_performance.c |
| F | 新トグル **Early Video Timing** (`speed.boot_display_early`)。既定 OFF | settings.c/h, main.c, app_state.c/h, page_performance.c |
| G | 起動時に `hw: GOGGLE_VER_2=%d revision=%d` を LOGI。**V1 のゲートは入れていない**(下記) | main.c |
| H | dm6302.c / main.c / hardware.c のコメント修正、拒否ログの errno を 2 回目のものに | 各所 |

### 9.2 G について方針変更

`GOGGLE_VER_2` は hardware.c:39 で `int GOGGLE_VER_2 = 1;` と初期化されるだけで、**実行時に代入する箇所が一つも無い**。つまりこのファームでは常に 1 で、`if (GOGGLE_VER_2 == 0)` の分岐は全部デッドコード(Goggle V1 用の名残)。よって V1 ゲートは意味が無いので入れず、版数のログだけ追加した。

ただし第 8.5.1 の「AL FPGA (0x65) が 1MHz で叩かれる」は残る。`pclk_phase_set()` の `I2C_Write(ADDR_AL, 0x14, ...)` は GOGGLE_VER_2 に関係なく実行され、Display_UI_init 経由で初期化窓に入る。

### 9.3 F の実装詳細と安全策

- 起動条件: `boot_display_early && async_display && boot_display && skip_boot_menu && boot_goes_to_hdzero_video()`。揃わないと `LOGW("vdpo: Early Video Timing needs ...")` を出して何もしない。
- **OLED_display() に「timing 変更が未回収の間は点灯しない」ガードを追加**。これが無いと OLED_Pattern が dispw の途中でパネルを点ける。ガードは on=1 のみ、`last_on` の更新より前に return するので、後の点灯は正常に効く。
- 暗いまま残らないよう回収点を 2 つ追加: `app_switch_to_hdzero` の CAM_MODE switch 直後(`default:` 経路対策)と、main.c の `start_running()` 直後。どちらも通常は no-op。
- 切り替え時の `start_display_timing_early()` は `vdpo_pending` が既に真なので即 return し、dispw は 2 回走らない。
- 起動時 CAM_MODE と切り替え時 CAM_MODE がずれた場合は `vdpo_set_timing` が先行分を join してから正しい方を同期実行する(遅くなるだけで正しい)。

### 9.4 次の検証 (実機、2 回起動)

**1 回目: Early Video Timing OFF のまま**(A〜D の回帰確認)
- `async_tuner` を OFF にして起動するのが一番はっきりする。
- 期待: `switch mark: audio and dvr while the display changes` が **出ない**。映像が dispw 終了の約 85ms 後。boot total が前回の 3559ms から **約 2870ms** に。
- `async_tuner` ON のままなら、この行は **出る**(dispw がまだ動いているので前倒しが正しく効く)。boot total は 2976ms 前後で変化なし。

**2 回目: Early Video Timing を ON、かつ Async Tuner Init も ON に戻す**(F の検証)

> **両方 ON が必須。** dispw を起動時に動かすと、切り替えで受信機初期化を隠してくれるものが無くなる。
> Early ON / Async Tuner OFF だと受信機の約 1.0 秒がむき出しになり、boot total は約 2830ms にしかならない(推定)。
> 2 つは互いを隠し合う関係ではなく、**両方を UI 構築の裏に入れて初めて**効く。

- 期待: `vdpo: 720p60 started in the background` が **`boot phase: lvgl` より前** に出る。
- `System Execute: Nms for dispw` が UI 構築中に完了。
- `HDZero: async open collected` の待ちが 300ms 以下。
- 切り替えで `vdpo: collected 720p60` が即座に。
- `switch mark: audio and dvr while the display changes` は **出ない**(dispw は終わっているので音声は映像の後ろに回る)。
- boot total **約 1900〜2100ms**(推定)。前回の 2500ms 見積もりは音声 535ms が映像前に残る前提の古いもので、A の修正でその分が後ろに回る。
- **見るべき異常**: 起動中に画面が点いて乱れないか。点かないのが正常(Skip Boot Menu で黒)。映像が出ないまま暗い場合は F を OFF に戻す。

### 9.5 設定の効き方の関係(実測と推定のまとめ)

映像までの時間。async_display は全ケース ON、spi_burst ON。

| Early Video Timing | Async Tuner Init | 映像まで | 根拠 |
|---|---|---|---|
| OFF | OFF | 約 2870ms | 実測 3408ms から A の 535ms を引いた推定 |
| OFF | ON | 2845ms | 実測 |
| ON | OFF | 約 2830ms | 推定。受信機がむき出しになる |
| ON | ON | **約 1900〜2100ms** | 推定 |

クリティカルパスは「初期化前半 約 500ms + UI 構築 約 1050ms + 末尾 85ms」に縮み、受信機も dispw も音声も全部その裏に入る。ここまで来ると次に効くのは **main_menu_init の 595〜766ms**(第 8.8 節の最大項目)。

**両方で共通に見る**
- `hw: GOGGLE_VER_2=1 revision=?`
- `SPI: burst refused` は 0〜1 回、`burst disabled` は出ない。`DM6302 after init` は left=0x0 right=0x0。
- `boot phase: display and osd` が 340〜430ms(1000ms 台なら I2C 独占の再発)。
- `wifi: stop skipped` の直後に killall の `System Execute` が 1 行(15ms 程度)。

---

## 10. 起動が「均衡」した状態の分析 (2026-09-09、Early Video Timing 実測後)

### 10.1 実測結果

| | 1回目 Early OFF | 2回目 Early ON |
|---|---|---|
| 映像まで | 3306ms | **1988ms** |
| 切り替え区間 | 1251ms | **130ms** |

boot total は 3435 / 2625ms だが、これは切り替え終了時点。音声設定 482ms が映像の後ろに回ったので「映像まで」とは別物。→ コミット 5be7083 で `boot: picture at Nms` を追加。

### 10.2 3 本のトラックが均衡している

2回目、アプリ開始 54.850 を 0 とする。

| トラック | 開始 | 終了 | 余裕 |
|---|---|---|---|
| メインスレッド(切り替え開始まで) | 0 | **1858ms** | 0(律速) |
| 受信機初期化ワーカー | 462ms | 1785ms | 73ms |
| dispw ワーカー | 497ms | 1620ms | 238ms |

映像 = 1858 + 130 = 1988ms。**3 本が 240ms 以内に収まっている。**

つまり 1 本だけ短くしても、次に長いものが顔を出すだけ。メニュー構築 976ms を丸ごと消しても、受信機(1785ms)が律速になり映像は 1915ms、**わずか 73ms しか縮まない**。

### 10.3 メニュー構築の内訳(2回目、合計 976ms)

Performance 155、Bug Fix + 末尾 143、WiFi Module 89、Power 62、Focus Chart 62、Image Setting 61、Firmware 53、OSD 45、Favorites 39、Source 34、Record Option 32、Input 30、Analog RSSI 24、Scan Now 23、Auto Scan 22、ELRS 20、Fans 16、Head Tracker 15、Clock 12、Storage 11、Playback 9、Go Sleep 5。

自作 3 ページ(Performance / Bug Fix / Favorites)で 337ms、全体の 1/3 強。Performance が単独最大。

### 10.4 遅延構築が「効果なし」だった理由と、今回の再挑戦

commit 43bf7b4 の記録: 「577ms を追い出したら display and osd が 327ms → 774ms になった。osd_init() がフォント先読みを待つ番になり、450ms が戻ってきた」。

**今日も同じ構図**。2回目のログでフォント先読みは 55.376 → 56.221(845ms)で、osd_init の回収 56.538 の 317ms 前にやっと終わっている。メニュー構築を外すと osd_init が 660ms 早く来るので、待ちが露出する。

対策としてコミット e68cb2a で:
- フォント先読みを `pclk_phase_init()` の直後(設定読み込み直後)へ移動。約 375ms 前倒しで、これが可能な最も早い位置。
- Defer Menu Build をトグルとして復活(既定 OFF、起動がメニューで終わる場合はスキップ)。

**見込みは 70〜240ms**。幅があるのは、受信機初期化の遅れのうちどれだけが CPU 競合かによる(第 8.4 節で EFUSE1 に +294ms、今回のログでは +242ms)。メインスレッドが空けば受信機も速くなり、その分だけ床が下がる。

### 10.5 これ以上速くするには

3 本すべてを削らないと意味がない。1400ms 台を狙うなら:

| トラック | 現在 | 必要 | 手段 |
|---|---|---|---|
| メインスレッド | 1858ms | ≤1370 | Defer Menu Build(−976)+ フォント前倒し。達成可能 |
| 受信機 | 1785ms | ≤1370 | EFUSE1 の 2 チップ同時読み(−330、第 4 章 #5)+ spawn をさらに前倒し |
| dispw | 1620ms | ≤1370 | 1121ms は固定。spawn を device_init 前(hw_stat_init 直後)へ移せば約 −185ms |

dispw の 1121ms は中身不明で短縮手段が無いため、**理論的な床は「dispw の開始時刻 + 1121ms + 130ms」**。最速でも spawn は iic_init 後なので、おおよそ 150 + 1121 + 130 = **1400ms 前後が限界**。

### 10.6 次の検証(1 回起動)

Early Video Timing と Async Tuner Init は ON のまま、**Defer Menu Build を ON** にして 1 回起動。

- `boot phase: menu pages 0ms (deferred)` と `boot phase: menu pages after video NNNms` が出る。
- `boot: picture at Nms` を前回の 1988ms と比べる。1750〜1920ms なら想定内。
- `boot phase: display and osd` が伸びていないか(フォント待ちが露出していないか)。前倒しが効いていれば 300ms 台のまま。
- メニューを開いて全ページが正常に描画されるか。映像の上に出ないか(`main_menu_move_behind_osd()` が効いているか)。

---

## 11. 総攻撃 (2026-09-09、コミット 6c23a33 / f4312cc)

### 11.1 前提: 1791ms の内訳(実測、Defer OFF)

アプリ開始 54.891 を 0 とする。

| トラック | 開始 | 所要 | 終了 |
|---|---|---|---|
| メインスレッド(切り替えまで) | 0 | | **1530ms** |
| 受信機ワーカー | 442ms | 1224ms | **1666ms** ← 律速 |
| dispw ワーカー | 484ms | 1101ms | **1585ms** |

映像 = 1666 + 125 = 1791ms。**映像 = 受信機の起動時刻 + 受信機の所要時間 + 約125ms**。

Defer Menu Build が効かなかったのはこのため。メインスレッドを 1530 → 1219ms に縮めても受信機が 1833ms で待たせるだけだった。

### 11.2 やったこと

**6c23a33: ワーカーの起動をバスが許す限り前倒し**

両ワーカーの起動を device_init() の中、TP2825_Config() と DM5680_req_ver() の直後へ移動。

- TP2825 は受信機と同じポート 2 なので、1MHz になる前に設定を終える必要がある → ワーカーより前。
- IT66121 はポート 1、IT66021 はポート 3。1MHz と無関係なので後ろへ回した(約 110ms)。
- language_init() と vclk/pclk_phase_init() はファイル解析のみでハードウェアに触れない。位相テーブルを読むのは切り替え時なので、ワーカーの後ろへ回した(約 100ms)。
- フォント先読みも settings_load() の直後へ。

**f4312cc: EFUSE を 2 チップ同時読み(トグル、既定 OFF)**

SPI_Read は元から両チップ分を返し、SPI_Write(sel=0) は両チップに書ける。にもかかわらず 94 バイト × 十数バンドをチップごとに 2 周していた。ready ビットを 1 個だけ見る利点と引き換えに、歩き全体を 2 回やっていた。

分岐しうる箇所は per-chip で追跡:
- `band_num` はチップごとに読み、ループは大きい方まで回す。
- バンド読み出しを打ち切る周波数チェック(j==3 と j==67)はチップごとにフラグを持ち、止まったチップにはそれ以降を書かない。

**RF 校正に触るので照合手段を入れた**。両経路の最後に構造体全体の FNV-1a 指紋と band_num / bandgap / ical / rcal を LOGI する。EFUSE は工場で一度焼かれるだけなので、正しく読めていれば毎回同じ指紋になる。遅い経路と速い経路で一致すれば正しい。

`MAX_PANELS` は 26 → 32(Performance ページが 27 行になったため)。追加した `_Static_assert` が超過を検出した。

### 11.3 見込み(推定)

| 段階 | 受信機終了 | dispw 終了 | メイン | 映像 |
|---|---|---|---|---|
| 現状(実測) | 1666 | 1585 | 1530 | **1791ms** |
| + 前倒しのみ | 1456 | 1337 | 1530 | 約 1655ms |
| + Defer Menu ON | 1456 | 1337 | 約1150 | 約 1581ms |
| + Dual-Chip EFUSE ON | 1126 | 1337 | 約1150 | 約 1462ms |

3 つ揃うと dispw(1337ms 終了)が床になる。それ以上は dispw の 1101ms が固定なので、起動を 150ms まで詰めても **1376ms 前後が限界**。

### 11.4 次の検証(2 回起動)

**1 回目**: 新ビルドをそのまま起動(Defer Menu Build と Dual-Chip EFUSE Read はどちらも OFF)。

- `boot: picture at` が **1655ms 前後**なら前倒しが効いている。
- **`EFUSE1 1: ... fingerprint=XXXXXXXX` と `EFUSE1 2: ... fingerprint=YYYYYYYY` を控える。** これが遅い経路の正解値。
- 映像が出たらメニューで **Defer Menu Build** と **Dual-Chip EFUSE Read** を両方 ON にして電源を切る。

**2 回目**: 両方 ON。

- `boot: picture at` が **1460ms 前後**か。
- **指紋 2 つが 1 回目と完全一致するか。** 違っていたら Dual-Chip EFUSE Read を即 OFF にすること。校正が壊れている。
- `EFUSE1 both, s1/s2/s3/s4` と `EFUSE1 both done` が出る。
- `boot phase: menu pages 0ms (deferred)` と `menu pages after video NNNms`。
- メニューを開いて全ページ正常か。映像の上に出ていないか。
- 受信機の感度に異常がないか(バンド端のチャンネルで映りが悪くないか)。

---

## 12. 結果 (2026-09-09、OFF/OFF と ON/ON の 2 回起動)

### 12.1 EFUSE の指紋は完全一致

| | 1回目(従来の逐次読み) | 2回目(2チップ同時読み) |
|---|---|---|
| チップ1 | band_num=2 bandgap=00002080 ical=1 rcal=1 **ea4dc8cf** | 同左 **ea4dc8cf** |
| チップ2 | band_num=2 bandgap=00002080 ical=4 rcal=0 **3c0f97a7** | 同左 **3c0f97a7** |

構造体全体のハッシュが一致。**2 チップ同時読みは校正データをビット単位で同一に読めている。**

EFUSE の所要: 逐次 637ms(349 + 288)→ 同時 422ms。**−215ms**。band_num=2 なのでバンド数が少なく、想定した −330ms までは届かなかった。

### 12.2 映像まで 1440ms(過去最速)

2回目、アプリ開始 54.911 を 0 とする。

| トラック | 開始 | 終了 |
|---|---|---|
| dispw | 179ms | **1298ms** |
| 受信機 | 106ms | **1300ms** |
| メインスレッド | 0 | **1313ms** |

映像 = 1313 + 127 = **1440ms**。**3 本が 15ms 以内に揃った。** 完全に均衡している。

この作業全体では 4819ms → 1440ms、**70% 減**。

### 12.3 3 つの設定は「セット」で効く(重要)

| 設定 | 映像まで |
|---|---|
| 前倒しなし・Defer OFF・逐次 EFUSE(前回) | 1791ms |
| **前倒しあり**・Defer OFF・逐次 EFUSE(1回目) | **2097ms** ← 悪化 |
| 前倒しあり・Defer ON・同時 EFUSE(2回目) | **1440ms** |

1回目が悪化した理由: 受信機ワーカーを 42ms に前倒ししたことで、EFUSE の 637ms がメインスレッドの IT66021/IT66121 初期化と重なった。同じ作業が 185ms → 406ms に伸びている。**シングルコアでは I2C を叩くスレッド同士は重ならない。**

2回目では EFUSE が 422ms に縮み、メニュー構築 525ms も外れたので、競合する相手が消えて均衡した。

**結論: Early Worker Start(順序変更、常時有効)は Defer Menu Build と Dual-Chip EFUSE Read が両方 ON のときだけ得。** どちらかを OFF に戻すなら、前回の 1791ms より遅くなる可能性がある。

### 12.4 残っているもの

映像まで 1440ms のうち **dispw が 1117ms**。開始が 179ms なので、これがほぼそのまま床。

- dispw の中身は短縮手段が無い(外部コマンド)。
- 開始をさらに早める余地は 179 → 約 100ms 程度。映像は 1350ms 前後が限界。
- メニュー構築(映像後に 397ms)、受信機初期化、音声設定はすべて隠れているので、これ以上速くしても映像には出ない。

**実質的に打ち止め。** これ以上は dispw が何をしているかを解析するしかない。

---

## 13. 切り替え側とメニュー再編 (2026-09-09、コミット cc454d7〜a30b025)

第 12 章で起動側は打ち止め(映像まで 1440ms、dispw 1117ms が床)。ここからは
**メニューに戻る側**と、Performance ページ自体の作り。ビルド済み、**実機未検証**。

### 13.1 きっかけ

ユーザー報告:「映像を受信しているときにメニューに戻すと、ビープが鳴ってから
2 秒くらいしてメニューに切り替わる。映像受信中でないと待ちはない」。

原因は `dvr_cmd(DVR_STOP)` の `sleep(2)`。信号取得で自動録画が始まる
(thread.c、Manual record OFF かつ SD あり)ので「映像受信中 = 録画中」。
`app_switch_to_menu()` は btn_press から **lvgl_mutex を保持したまま**呼ばれる
ので、この 2 秒は `lv_timer_handler()` が一切描画できない 2 秒。録画していなけ
れば `if (dvr_is_recording)` に入らないので待ちゼロ。報告の切り分けと一致。

### 13.2 やったこと

| コミット | 内容 | ファイル |
|---|---|---|
| cc454d7 | 効かない行のグレーアウト。Keep Tuner Alive(Menu Over Video ON 時)、Early Video Timing(依存 3 つ未充足時)、Long Press Time(Timed Long Press OFF 時)が `(no effect)` 表示になる。Throttle UI Updates と Skip Idle Redraws を 1 行に統合(両方ステータスバーのみが対象で直列関係) | page_performance.c |
| fc26715 | **Poll DVR Stop**。`sleep(2)` を `/tmp/record.dat` の 20ms ポーリングに。record プロセスは `ffpack_close()` の後に状態を書くので、それがクローズ完了の本人申告。2000ms は上限として維持 | dvr.c, settings.c/h |
| 92cbbdb | **Poll DVR Start**(開始側の同型。`dvr_mutex` を握ったまま寝るので切り替え側も止めていた)と **Async Menu Display**(1080p50 を `app_switch_to_menu()` 冒頭で非同期起動し、録画停止・音声ミュート・ライブ停止を dispw の裏に回す。`hdzero_recording_side()` と同じ形) | dvr.c, app_state.c, settings.c/h |
| c949f31 | `create_select_item()` に行数を渡す。隠しパネル **704 → 221**。全ページが `GRID_ROWS(row_dsc)` を渡す | page_common.c/h + 全 20 呼び出し元 |
| b4b7bc8 | Performance(31/32 行)を **Boot Speed / Switch Speed / Input Feel** に分割 | 新規 4 ファイル、page_performance.* 削除 |
| a30b025 | Bug Fix → **Fixes**(表示名のみ、ini セクションは `bugfix` のまま) | page_bugfix.c |

### 13.3 メニュー構成

| ページ | 行数/32 | 行高 | スクロール |
|---|---|---|---|
| Boot Speed | 10 | 60px | なし |
| Switch Speed | 16 | 51px | あり |
| Input Feel | 7 | 60px | なし |
| Fixes | 2 | 60px | なし |

分割の軸は**いつ効くか = どう検証するか**。Boot Speed は焼いて再起動してログ、
Switch Speed はボタンを押す。副産物として、グレーアウトする 3 行がすべて
依存元と同一ページ・直下に並んだ。

チューナ 3 項目は効き方で分けた: Async Tuner Init は起動時の init しか動かさ
ないので Boot Speed、Burst / Dual-Chip EFUSE は毎回の init を短くするので
Switch Speed。

サイドバー 2 ページ目は 5 エントリ(約 19 枠中)。`MENU_STOCK_COUNT` は
`PAGE_COUNT - 5`。ini キーは一切変えていないので設定は引き継がれる。

### 13.4 次の検証(実機)

**A. 切り替えの待ち(録画中に映像 → メニュー)**
- Poll DVR Stop ON で `dvr: record stop took Nms` が 2000ms より大幅に短いか。
- Poll DVR Start ON で `dvr: record start took Nms`。
- Async Menu Display ON で
  `switch mark: audio and live stop while the display changes` が出るか。
- **目で見る項目**: Async Menu Display はパネル消灯が `vdpo_start_timing_async()`
  の時点に早まる。映像が切れてからメニューが出るまでの黒い間の見え方が変わる。
  ログでは判断できない。

**B. メニュー再編の回帰**
- 4 ページが崩れずに出るか。Switch Speed の 16 行スクロール、Boot Speed /
  Input Feel の説明行が切れていないか。
- サイドバー 2 ページ目の 5 エントリ、両端でのページ切り替え。
- `boot phase: menu pages Nms` — パネル 704→221 の効果。第 10.3 節では
  976ms(Defer OFF)。

**C. 回帰していないこと**
- 全トグル OFF で従来どおりか。Async Menu Display OFF 時は
  `menu_recording_side()` が末尾で従来と同じ順序(line_out → REC_STOP_LIVE)
  で走る。並べ替えの条件はトグルにも掛けてあるので、他経路の dispw が
  走っていても OFF なら順序は変わらない。

### 13.5 手を付けなかったもの

- `dvr_cmd()` 開始側の `usleep(100 * 1000)`(REC_START 直前)。待つ対象の信号が
  なく、置き換える根拠がない。
- `dvr_update_status()` の `sleep(2)`。record プロセスが予期せず落ちたときしか
  通らない。
- `create_select_item()` の行数化には**トグルを付けていない**(ユーザー了解済み
  の例外)。UI は完全に同一で戻す対象の体験がなく、行数の過少申告は設定で切る
  ものではなく欠陥だから。単独コミット c949f31 なので revert 一発。
- 第 2 章(不具合)・第 3 章(リスク)の個別項目が A〜H に吸収されたのか未消化な
  のかは棚卸ししていない。特に **3.11「Performance ページの見出しスクロールが
  コメント通りに動いていない」** は、そのスクロール処理を Switch Speed に
  `speed_scroll_to()` としてそのまま引き継いだので、未修正なら同じ症状が残る。

---

## 14. 実機 1 回目の結果 (2026-09-08 04:01-04:03 のログ、コミット 8fcdb08 のビルド)

全トグル ON で起動。ログの日付が 9/8 なのは RTC の電池切れで毎回同じ保存値に
戻るため(第 0.3 節)。

### 14.1 メニュー切り替えは実質ゼロになった

`switch mark: to_menu start` から `menu shown` まで:

| 状況 | menu shown |
|---|---|
| 録画していない | **7ms / 17ms** |
| 録画 3 秒以上 | 128ms / 136ms / 143ms |
| 録画 3 秒未満 | 1467ms / 2029ms / 2030ms |

**切り替えそのものは 10〜25ms。** 残りは全部 `dvr_cmd(DVR_STOP)` の待ち。
Menu Over Video が ON なので `Display_UI()` の 1.1 秒は最初から走っていない。

### 14.2 Poll DVR Start は効いている、Poll DVR Stop は上限に当たる

`record start took`: 484 / 81 / 142 / 123 / 101 / 148 / 101 / 122ms。全部 2000ms
より大幅に短い。

`record stop took` を、直前の録画がどれだけ続いたかと並べると:

| 録画継続 | stop 待ち | 合計 |
|---|---|---|
| 6.09s | 101ms | 6.19s |
| 4.26s | 121ms | 4.38s |
| 3.35s | 121ms | 3.47s |
| 1.49s | 1451ms | **2.94s** |
| 0.74s | 2012ms | 2.75s(上限) |
| 0.70s | 2017ms | 2.72s(上限) |
| 0.28s | 2011ms | 2.29s(上限) |
| 0.25s | 2011ms | 2.26s(上限) |

**録画プロセスは開始から約 3 秒経つまで確定しない。** それより早く止めると残りを
待たされ、2000ms の上限に当たる。上限は元の `sleep(2)` と同じなので悪化はして
いないが、短い録画では待ちが残る。

これがユーザー報告の 2 件の正体:
- 「映像が出てすぐメニューに戻すと遅い」= 自動録画開始から 3 秒以内
- 「映像が見えている時にチャンネル変更すると 2 秒待つ」= `tune_channel` の
  DIAL_KEY_CLICK も `dvr_cmd(DVR_STOP)` を通る(input_device.c:176)

### 14.3 Async Menu Display は一度も発火していない

`switch mark: audio and live stop while the display changes` が 0 回。
`keep_display` が ON だと `overlay` が真になり、`!overlay` のブロックごと飛ばす。
設計どおり(表示を変えないので前倒しする対象が無い)だが、行がそう言っていない
ので `row_inert()` に追加した。

### 14.4 レイアウト崩れ (Boot Speed / Input Feel で Back が見えない)

グリッドはテーマの `pad_row`(約 11px)を行間に入れる。`style_context` はこれを
上書きしていないので、`ROW_COUNT * ROW_H` で container を作ると行数−1 個分の
gap だけ足りない。

- Boot Speed: 10×60 + 9×11 = 699px を 600px の箱に → Back(行 9)は 639〜699 で
  完全に箱の外
- Input Feel: 7×60 + 6×11 = 486px を 420px の箱に → Back は 426〜486 で外
- Switch Speed: スクロールするので露呈しなかった。旧 Performance も同じ

対処: 3 ページとも Switch Speed と同じジオメトリ(`SPEED_LIST_H`)にし、
`speed_page_begin()` で全ページにスクロールを付けた。高さの見積もりを外しても
最後の行が消えるのではなくスクロールする。

### 14.5 入れた対策

| コミット | 内容 |
|---|---|
| (この節) | レイアウト: 3 ページ共通ジオメトリ + 全ページスクロール |
| (この節) | **Defer DVR Stop** (`speed.dvr_defer_stop`、既定 OFF)。停止を投げっぱなしにし、待ちを次の録画開始へ移す。間に走るのはチャンネル変更と表示タイミングで、どちらも録画プロセスのものではない |
| (この節) | `row_inert` に 2 件追加: Async Menu Display(Menu Over Video ON 時)、Poll DVR Stop(Defer DVR Stop ON 時) |

### 14.6 次の検証

- **Boot Speed / Input Feel で Back が見えるか。** 最優先
- Defer DVR Stop ON で、チャンネル変更と「映像直後のメニュー」の 2 秒が消えるか。
  ログの `dvr: record stop left to finish in the background` と
  `dvr: record stop collected took Nms`(次の録画開始時)で追える
- **未検証の危険**: 録画プロセスがまだファイルを閉じている最中に dispw と
  チャンネル変更が走る。VIPP からフレームを引いている最中の表示再構成が安全か
  はソースからは判断できない(Allwinner SDK バイナリ)。録画ファイルが壊れて
  いないか、SD の映像を再生して確認すること

---

## 15. 実機 2 回目 (2026-09-09、コミット a6311ce のビルド)

### 15.1 Defer DVR Stop: 録画ファイルは壊れていない

ユーザー確認: 「映像が壊れているように見えませんでした」。第 14.6 節で挙げた
「録画プロセスがファイルを閉じている最中に dispw が走る」危険は、少なくとも
この試行では顕在化せず。**1 回の確認なので、断続的な破損の可能性は残る。**
使い込む中で再生できないファイルが出たら真っ先に疑う対象。

### 15.2 Playback がオーバーレイ中に壊れる (第 3.4 節の実機確認)

症状: 「playback で動画を流すと、映像のラインずれというか解像度が合っていない
ような表示」。

原因:
- `ui_player.h` の `MPLAYER_SCR_WIDTH/HEIGHT` は 1920x1080 固定。プレイヤーは
  その大きさでレイヤーを作り、デコードしたフレームを SoC の VO に渡す。
- Menu Over Video だと `app_switch_to_menu()` が `Display_UI()` を呼ばないので、
  FPGA レジスタ 0x20 はライブ映像源を選んだまま(`Display_VO_SWITCH(1)`)、
  パネルは 720p60 のまま、LVGL も 720p のまま。
- そこへ 1080p のレイヤーと VO のフレームが来るので、ずれて拡大率も合わない。
- `page_playback.c` には表示モードを触る処理が一切無い。stock ではメニューに
  入った時点で `Display_UI()` が済んでいるので問題にならなかった。

対処: `app_menu_end_overlay()` を app_state.c に追加し、`page_playback_enter()`
から呼ぶ。オーバーレイ中だったときだけ、スキップされた切り替え
(`Display_UI()` + `lvgl_switch_to_1080p()` + チューナ停止 + アナログ電源断)を
そこで実行する。メニューを出るときは `app_switch_to_hdzero()` が表示もチューナ
も設定し直すので、戻す処理は不要。

**新しいトグルは付けていない。** この不具合は Menu Over Video が ON のときしか
起きず、それを OFF にすれば消える。既存のスイッチが revert になる(第 3.7 節と
同じ形)。

代償: Menu Over Video ON で Playback ページに入ると dispw の約 1.1 秒がかかる。
`.enter` は lvgl_mutex を保持したまま呼ばれるので、その間は描画が止まる。
ファイル一覧を見るだけでも払うことになるが、再生ボタンは即座になる。

### 15.3 次の検証

- Playback の表示が正常か(Menu Over Video ON で)
- Playback ページに入るときの約 1.1 秒が許容できるか。気になるなら
  `mplayer_file()` の直前に移す手もある(一覧は速いまま、再生開始が遅くなる)
- Playback から戻ってメニューを続けると、そのセッションはオーバーレイでなく
  通常のメニュー表示になる。違和感がないか
- Boot Speed / Input Feel の Back が見えるか(第 14.4 節の修正、未確認)

### 15.4 720p Playback (第 15.2 節の代案)

ユーザーの問い:「playback 画面を 720p のまま視聴できませんか?」

**できる。** 第 15.2 節の修正は 1080p に戻すために dispw の 1.1 秒を払うが、
その 1.1 秒は `Display_UI_init()` の中の `vdpo_set_timing(1080p50)` だけで、
他は全部 I2C と aww で数十 ms。そして `OLED_SetTMG` のコメントが
`mode: 0=1080P; 1=720P` と言っているとおり、**パネルのタイミングは解像度で
決まり、UI か映像かでは決まらない**。映像経路が既に `OLED_SetTMG(1)` を設定
しているので、720p の UI に必要な部品は揃っている。

壊れていた理由は vdec2vo.c:225 の一箇所:
```c
voLayerAttr.stDispRect.Width  = config->vo.width;   // media.c の VO_WIDTH = 1920
voLayerAttr.stDispRect.Height = config->vo.height;  // VO_HEIGHT = 1080
```
`stDispRect` はデコードした映像を表示面に描く矩形。パネルが 1280x720 のときは
1.5 倍の矩形を 720p の面に描くので、出力 1 行あたり 1.5 行分が入って斜めにずれる。
報告の「ラインずれ」と一致。

入れたもの:
- `Display_UI_keep_timing()` (hardware.c)。dispw だけ飛ばし、FPGA ソース選択・
  クロック位相・OLED モードは通常どおり動かす。LVGL も 720p のまま
- 1080p 決め打ちを 2 箇所とも実解像度に: `ui_player.c` の `MPLAYER_SCR_*`
  (720p では操作バーが画面下端の 200px 下にあった)、`media.c` の `VO_*`。
  1080p では同じ数値なので、トグル OFF なら挙動不変
- 新トグル **720p Playback** (`speed.playback_keep_timing`、既定 OFF)。
  Menu Over Video が OFF ならグレーアウト(メニューが既に表示を取っている)

**未確認の不確かさ:** FPGA の 0x84 / 0xa7 / 0x8C とクロック位相について、UI 用の
値が 720p60 のタイミングで妥当かはソースから判断できない。ハードがこの組み合わせ
で動いたことが無い可能性もある。理屈と症状は一致しているが、実機で見るまでは推測。
駄目なら OFF で第 15.2 節の動作(1.1 秒かけて 1080p)に戻る。

---

## 16. 実機 3 回目 (2026-09-09、コミット de191ff のビルド)

### 16.1 起動が 12957ms かかった — 原因特定

`BOOT_STEP` が仕事をした。

```
boot phase: display and osd 10280ms
  boot step: oled startup 9955ms      ← ここ
boot total: app start to switch done 12957ms   (通常 2363〜2513ms)
```

その 10 秒間:
```
04:01:55.804  M0 write  64/237 at    31ms
04:02:00.855  M0 write 128/237 at  5083ms
04:02:05.920  M0 write 192/237 at 10147ms
04:02:05.948  boot step: oled startup 9955ms
```

**チューナの M0 転送と `OLED_Startup()` が同じ I2C バスで衝突し、両方が約 100 倍
遅くなった。** 通常は M0 92ms / OLED 94ms(now.log, run2.log で確認)。

原因は**速度**。`DM6302_init()` は実行中ずっとメイン I2C バスを 1MHz に上げる
(`DM6302_BUS_1MHZ`)。OLED は FPGA 経由(`ADDR_FPGA` の 0xa8/0xa9/0xaa/0xad/0xae)
で読み書きするので、**FPGA が 1MHz で応答できない**。1 転送が本来 30µs のところ
約 39ms かかっている計算で、クロックストレッチかリトライ。第 8.5.1 節で挙げた
懸念がそのまま出た。

`i2c.c` のターンストルは両者を公平に分け合わせるが、それは飢餓を防ぐためのもの
で、FPGA が答えない速度の転送を速くはできない。

**対処:** `OLED_Startup()` の直前で `HDZero_open_async_wait()` を呼び、ワーカーの
完了を待つ。M0 が終われば `DM6302_init()` がバスを 200kHz に戻すので、そこから
OLED は全速。既に終わっている起動(大半)では待ちゼロ。ログの
`boot step: wait for the tuner bus Nms` で何回に 1 回どれだけ待ったかが見える。

新トグルは付けていない。`Async Tuner Init` を OFF にすれば重なり自体が消えるので、
既存のスイッチが revert になる(第 3.7 / 15.2 節と同じ形)。

**この修正の限界:** 待つのは `OLED_Startup()` の前だけ。同じバスを使う他の
初期化(`Display_UI_init()` の `pclk_phase_set()` は `I2C_Write(ADDR_AL, 0x14, ...)`
を通る)は、ワーカーがそこまでに終わっている前提のまま。今回の順序では
`OLED_Startup()` が最初なので、そこで待てば以降は安全。順序を変えるなら再考が必要。

### 16.2 720p Playback は実機で失敗

ユーザー: 「720p Playback を ON にして Playback したら表示がおかしくなっちゃい
ました」。第 15.4 節で「FPGA の 0x84 / 0xa7 / 0x8C とクロック位相が 720p60 の
タイミングで UI 用の値のまま妥当かは判断できない」と書いた部分が、妥当でなかった
ということ。

16.1 で「FPGA には速度・タイミングの制約があり、想定外の組み合わせでは素直に
壊れる」ことが裏づけられたので、同種の問題と見てよい。

**症状(ユーザー報告):** 画像がラインごとに横ずれ、横方向は画面いっぱい、縦は上
1/4 程度、紫が強い、まばらなランダムピクセル。長押ししてもメニューに戻れず、画面
が変わらないか真っ暗。

**特定できたバグ:** `lv_disp_get_hor_res()` は**パネルの解像度ではない**。
`ui_porting.h` の `DRAW_HOR_RES_HD = 1280 + DISP_OVERSCAN(8) = 1288`、
`DRAW_VER_RES_HD = 728`。LVGL の描画バッファはオーバースキャン込み。これを
`stDispRect` に渡したので、パネルが 1 行 1280 なのに 1288 として書かれ、1 行ごとに
8px ずつずれる = 報告の「ラインごとの横ずれ」。

**さらに:** 同じ誤りは 1080p でも起きていた。`DRAW_HOR_RES_FHD` は 1928x1088 で
1920x1080 ではない。コミット 7a1c887 のメッセージに「1080p では同じ数値」と書いた
のは誤り。トグル OFF でも Playback の矩形が 8px ずれていたことになる。

**撤去した(コミット 5c8d33a 前後)。** 紫・ランダムピクセル・縦 1/4 は矩形のずれ
だけでは説明がつかず、色フォーマットや転送サイズなど複数が食い違っている。決め手は
**メニューに戻れなかった**こと: 飛行に使う機材で、ON にすると抜けられなくなりうる
スイッチを既定 OFF でもメニューに残すべきではない。

**再挑戦するなら:** まず 1080p のまま `stDispRect` を 1920x1080 に直す小さく確実な
修正から。それが正しく映ることを確認してから 720p を試す。切り分けの目安:
- 真っ黒／何も出ない → FPGA ソース選択(0x20)かパネルのタイミング
- 前と同じ斜めずれ → 矩形サイズがまだ合っていない
- 色がおかしい・ちらつく → クロック位相(`VIDEO_SOURCE_MENU_UI`)
- 縦横比だけ違う → スケーリング

追う価値が無ければトグルごと削除する。既定 OFF なので実害は無いが、動かない
スイッチを残すのは良くない。Playback 自体は第 15.2 節の版(1.1 秒かけて 1080p)で
正しく映ることが確認済み。

### 16.3 問題なかったもの

- Favorites CH の Source 行(HDZero / Analog 切り替え)
- Long Press Time の 50ms
- メニューのレイアウト(第 14.4 節の修正、全ページ確認済み)


### 16.4 Playback の一覧画面が左上に縮小される (第 15.2 節の修正の欠陥)

ユーザー: 「playback の動画選択一覧画面(サムネイルが並んでいる)のサイズが左上に
よって縮小されているように見えます。動画自体のサイズは問題ありません。」

**app_menu_end_overlay() の欠陥。** メニューの縮小率は `main_menu_show()` の中で
`main_menu_fit_display()` が一度だけ計算し、その後は誰も再計算しない。

```
1. 720p 映像を視聴            LVGL 728
2. メニューに入る(オーバーレイ) LVGL 728 のまま
                              main_menu_fit_display() -> zoom = (728-8)*256/1080 = 170 (2/3)
3. Playback に入る            app_menu_end_overlay() -> lvgl_switch_to_1080p() -> LVGL 1088
4. fit_display() は呼ばれない  メニューは 1080p 画面に 2/3 のまま = 左上に縮小
```

対処: `main_menu_refit_display()` を公開し、`app_menu_end_overlay()` が解像度を
変えた直後に呼ぶ。1088 > 設計値 1080 なので倍率は等倍に戻る。あわせて
`main_menu_apply_antialiasing()` も呼び直す — `menu_scaled` が true から false に
変わるため(第 2.1 節と同じ性質の見落とし)。

新トグルなし。`Menu Over Video` OFF でこの経路自体が通らない。

**一度誤診した記録:** 最初「録画が 720p で表示面が 1080p だから拡大されていない」
と答えた。動画本体の話だと思い込んだため。`vdec2vo.c` の `stImageSize` を疑ったが、
動画自体は正常に見えており、そちらは無関係だった。症状の主語を確かめずに機構を
説明した失敗。

---

## 17. 第 2 章・第 3 章の棚卸し (2026-09-09 実施、2026-09-10 更新)

第 0.4 章の A〜H は第 2/3 章の**個別項目を対象にしていなかった**。18 件を 1 件ずつ
現行コードに当てた結果、当時は全件が未修正だった。以降で 6 件が片付き、残り 12 件。

### 17.1 片付いたもの

| # | 項目 | コミット |
|---|---|---|
| 2.6 | `elapsed_ms()` が壁時計 | `eb6f3f6` |
| 3.4 | オーバーレイ中の Playback | `4ea6d5e` (実機で再現を確認してから修正) |
| 3.7 | `skip_boot_menu` が背景を黒のまま | `8fcdb08` |
| 3.10 | INI のお気に入り値が範囲チェックなし | `45dff4d` (+ `settings.c` の代入先間違いも) |

棚卸しの副産物として、`c949f31` で自分が入れた NULL 参照 (`2199d90`) と、
第 16.1 章の起動バス競合を発見した。

### 17.2 残り 12 件

**効果が明確 (次にやるならここ):**

- **2.1 アンチエイリアスが映像に戻っても復帰しない** — `main_menu_show(false)` が
  どこからも呼ばれないので復帰パスに到達しない。Menu Antialias OFF + Menu Over Video
  + 720p 映像で、映像に戻っても OSD と全ウィジェットがギザギザのまま。
  なお `417950c` で `main_menu_refit_display()` が
  `main_menu_apply_antialiasing()` を呼ぶようになったので、Playback 経由では
  部分的に直っている可能性がある。**まず現状を実機で確認すること。**
- **2.4 ステータスバーの差分判定が省略記号付きラベルで常に失敗** — `sb_label()` は
  `lv_label_get_text()` と比較するが、STS_SDCARD 以外の全ラベルが `LV_LABEL_LONG_DOT`。
  幅 267px を超えると LVGL が内部文字列を「…」で上書きするので毎回不一致 → 20Hz で
  全画面再描画 (拡大メニューなら 5.6MB レイヤーの再生成)。バー側で最後に設定した
  文字列を保持して比較する。

**設計判断が要る:**

- 3.1 HDMI 経路が dispw の記録を迂回 (直接 `system_exec("dispw ...")` が 7 箇所)
- 3.2 スタンバイ受信機が HDMI セッションや OLED 保護中も通電
- 3.3 オーバーレイ中の OLED 保護 → 復帰時にメニューが約 2 秒固まる
- 3.5 `thread_version` が `lvgl_mutex` なしで `lv_timer_handler()` を呼ぶ

**軽微・限定的:**

- 2.2 オーバーレイ中にカメラ解像度が変わると OSD がメニューの上に出る
- 2.3 お気に入りの帯域外スロットが「L1」等と表示される
- 2.5 IMU 初期化失敗時もゲートを開ける
- 3.6 `timed_long_press` がリピートイベント依存
- 3.8 `boot_fonts` が SD マウント前にフォントを読む可能性
- 3.11 見出しスクロールが効いていない (`speed_scroll_to()` に引き継ぎ済み、見た目のみ)
- 3.12 確定前の編集がメモリだけに残る

**検査不能:** 3.9 `skip_audio` の前提 (record デーモンがミキサーを触らない) —
外部バイナリ依存。
